
# #launch文件中usb port的端口号

rm -rf ~/ros2_ws/build/xf_mic_asr_offline ~/ros2_ws/install/xf_mic_asr_offline ~/ros2_ws/log

colcon build --packages-select xf_mic_asr_offline_msgs
colcon build --packages-select xf_mic_asr_offline

ros2 launch xf_mic_asr_offline mic_init.launch.py language:=Chinese  #启动语音节点

appid:8439bacb   科大讯飞申请的：2025/6/6：a6f03796   2025/9/7：86df9570，如果过期，要在科大讯飞官网申请离线语音包，把src\xf_mic_asr_offline\launch\mic_init.launch.py里面appid修改，并且src\xf_mic_asr_offline\config\msc\res\asr\common.jet替换为科大讯飞官网申请的



# 启动ros2 launch xf_mic_asr_offline voice_control_move.launch.py

编译好两个功能包，然后运行前先运行下面三个指令设定变量，
export need_compile='True'

export MACHINE_TYPE='MentorPi_Mecanum'

export ASR_LANGUAGE='Chinese'

然后运行下面指令

ros2 launch xf_mic_asr_offline voice_control_move.launch.py

通过这个指令查看运动指令

ros2 topic echo /controller/cmd_vel

